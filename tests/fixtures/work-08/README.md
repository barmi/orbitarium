# Work 8 Golden Fixtures

> SimulationClock + presets reference. `pnpm fixtures:work-08` 로 재생성.

## 파일

- `time-presets.json` — TS / Python 양쪽이 같은 jdTdb 생성하는지 cross-check.

## 갱신

```bash
pnpm fixtures:work-08
```

수동 갱신만. Preset 추가 / 변경 시 결정 로그에 먼저 기록.
